<h2>edit the publications</h2>
<%= simple_form_for [@place, @publication] do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit class: "btn btn-outline-primary" %>
<% end %>
